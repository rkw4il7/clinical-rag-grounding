# Streamlit's per-file ceiling (MB). Kept equal to the app's UPLOAD_MAX_MB default
# (200) so the uploader's built-in "Limit … per file" text matches the app cap.
# To allow larger uploads, raise BOTH this and UPLOAD_MAX_MB (.env).
[server]
maxUploadSize = 200
